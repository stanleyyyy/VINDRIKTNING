#pragma once

//
// wifi credentials
//

#define WIFI_SSID "YOUR_SSID_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// ignore changes with: git update-index --assume-unchanged src/config/secrets.h
// undo with: git update-index --no-assume-unchanged src/config/secrets.h
